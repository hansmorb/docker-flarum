# Domain where Flarum will be hosted
DOMAIN=localhost

# Container restart behavior (options: no, always, on-failure, unless-stopped)
RESTART_POLICY=unless-stopped

# ===================================================================
# MariaDB Database Configuration
# ===================================================================

# MariaDB image version (see tags: https://hub.docker.com/_/mariadb)
MARIADB_IMAGE_TAG=latest

# Path to store database files (relative to compose file location)
MARIADB_DATA_PATH=./volumes/mariadb

# Database superuser credentials (REQUIRED - change these in production!)
MYSQL_ROOT_PASSWORD=your_strong_root_password

# Application database name (will be created automatically)
MYSQL_DATABASE=flarumdb

# Application database user (limited privileges recommended)
MYSQL_USER=flarumuser

# Application database password (should be different from root password)
MYSQL_PASSWORD=your_strong_db_password

# ===================================================================
# Flarum Application Settings
# ===================================================================

# Flarum version (see releases: https://github.com/flarum/flarum/releases)
FLARUM_VERSION_TAG=v1.8.1

# Host port mapping for Flarum container (8888 is internal container port)
FLARUM_PORT=8080

# Base URL for your forum (MUST match your domain and port configuration)
# Example with HTTPS: https://${DOMAIN}
# Example with custom port: http://${DOMAIN}:9000
FLARUM_URL=http://${DOMAIN}:${FLARUM_PORT}

# Forum title (displayed in browser tab and header)
FLARUM_FORUM_TITLE="My Flarum Community"

# ===================================================================
# Flarum Administrator Account
# ===================================================================
# These credentials will create the initial admin account during setup
# (Change immediately after first login!)

FLARUM_ADMIN_USER=admin
FLARUM_ADMIN_PASSWORD=ChangeMeNow!123
FLARUM_ADMIN_EMAIL=admin@example.com

# ===================================================================
# Volume Storage Paths
# ===================================================================
# All paths are relative to docker-compose location
# Docker will create these automatically if they don't exist

# Database storage
MARIADB_DATA_PATH=./volumes/mariadb

# Flarum assets (user uploads, avatars)
FLARUM_ASSETS_PATH=./volumes/flarum/assets

# Application logs (debugging and error tracking)
FLARUM_LOGS_PATH=./volumes/flarum/logs

# Community extensions (plugins)
FLARUM_EXTENSIONS_PATH=./volumes/flarum/extensions

# Custom packages
FLARUM_PACKAGES_PATH=./volumes/flarum/packages